webpackJsonp([20],{65:function(s,a,t){var e,l;__vdoc_config_=t(98),l=t(137),s.exports=e||{},s.exports.__esModule&&(s.exports=s.exports["default"]),l&&(("function"==typeof s.exports?s.exports.options:s.exports).html=l)},98:function(s,a){},137:function(s,a){s.exports='<p>Vue.js 的模板是基于 DOM 实现的。这意味着所有的 Vue.js 模板都是可解析的有效的 HTML，且通过一些特殊的特性做了增强。Vue 模板因而从根本上不同于基于字符串的模板，请记住这点。</p> <h2 id=->插值</h2> <h3 id=->文本</h3> <p>数据绑定最基础的形式是文本插值，使用 &quot;Mustache&quot; 语法（双大括号）：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>span</span>&gt;</span>Message: {{ msg }}<span class=tag>&lt;/<span class=name>span</span>&gt;</span></div></pre></td></tr></table></figure><p>Mustache 标签会被相应数据对象的 <code>msg</code> 属性的值替换。每当这个属性变化时它也会更新。</p> <p>你也可以只处理单次插值，今后的数据变化就不会再引起插值更新了：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>span</span>&gt;</span>This will never change: {{* msg }}<span class=tag>&lt;/<span class=name>span</span>&gt;</span></div></pre></td></tr></table></figure><h3 id=-html>原始的 HTML</h3> <p>双 Mustache 标签将数据解析为纯文本而不是 HTML。为了输出真的 HTML 字符串，需要用三 Mustache 标签：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span>&gt;</span>{{{ raw_html }}}<span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure><p>内容以 HTML 字符串插入——数据绑定将被忽略。如果需要复用模板片断，应当使用 <a href=/api/#partial>partials</a>。</p> <p class=tip>在网站上动态渲染任意 HTML 是非常危险的，因为容易导致 <a href=https://en.wikipedia.org/wiki/Cross-site_scripting>XSS 攻击</a>。记住，只对可信内容使用 HTML 插值，<strong>永不</strong>用于用户提交的内容。</p> <h3 id=html->HTML 特性</h3> <p>Mustache 标签也可以用在 HTML 特性 (Attributes) 内：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>id</span>=<span class=string>"item-{{ id }}"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure><p>注意在 Vue.js 指令和特殊特性内不能用插值。不必担心，如果 Mustache 标签用错了地方 Vue.js 会给出警告。</p> <h2 id=->绑定表达式</h2> <p>放在 Mustache 标签内的文本称为<strong>绑定表达式</strong>。在 Vue.js 中，一段绑定表达式由一个简单的 JavaScript 表达式和可选的一个或多个过滤器构成。</p> <h3 id=javascript->JavaScript 表达式</h3> <p>到目前为止，我们的模板只绑定到简单的属性键。不过实际上 Vue.js 在数据绑定内支持全功能的 JavaScript 表达式：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line>{{ number + 1 }}</div><div class=line></div><div class=line>{{ ok ? \'YES\' : \'NO\' }}</div><div class=line></div><div class=line>{{ message.split(\'\').reverse().join(\'\') }}</div></pre></td></tr></table></figure><p>这些表达式将在所属的 Vue 实例的作用域内计算。一个限制是每个绑定只能包含<strong>单个表达式</strong>，因此下面的语句是<strong>无效</strong>的：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=comment>&lt;!-- 这是一个语句，不是一个表达式： --&gt;</span></div><div class=line>{{ var a = 1 }}</div><div class=line></div><div class=line><span class=comment>&lt;!-- 流程控制也不可以，可改用三元表达式 --&gt;</span></div><div class=line>{{ if (ok) { return message } }}</div></pre></td></tr></table></figure><h3 id=->过滤器</h3> <p>Vue.js 允许在表达式后添加可选的“过滤器 (Filter) ”，以“管道符”指示：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line>{{ message | capitalize }}</div></pre></td></tr></table></figure><p>这里我们将表达式 <code>message</code> 的值“管输（pipe）”到内置的 <code>capitalize</code> 过滤器，这个过滤器其实只是一个 JavaScript 函数，返回大写化的值。Vue.js 提供数个内置过滤器，在后面我们会谈到如何开发自己的过滤器。</p> <p>注意管道语法不是 JavaScript 语法，因此不能在表达式内使用过滤器，只能添加到表达式的后面。</p> <p>过滤器可以串联：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line>{{ message | filterA | filterB }}</div></pre></td></tr></table></figure><p>过滤器也可以接受参数：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line>{{ message | filterA \'arg1\' arg2 }}</div></pre></td></tr></table></figure><p>过滤器函数始终以表达式的值作为第一个参数。带引号的参数视为字符串，而不带引号的参数按表达式计算。这里，字符串 <code>&#39;arg1&#39;</code> 将传给过滤器作为第二个参数，表达式 <code>arg2</code> 的值在计算出来之后作为第三个参数。</p> <h2 id=->指令</h2> <p>指令 (Directives) 是特殊的带有前缀 <code>v-</code> 的特性。指令的值限定为<strong>绑定表达式</strong>，因此上面提到的 JavaScript 表达式及过滤器规则在这里也适用。指令的职责就是当其表达式的值改变时把某些特殊的行为应用到 DOM 上。我们来回头看下“概述”里的例子：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>p</span> <span class=attr>v-if</span>=<span class=string>"greeting"</span>&gt;</span>Hello!<span class=tag>&lt;/<span class=name>p</span>&gt;</span></div></pre></td></tr></table></figure><p>这里 <code>v-if</code> 指令将根据表达式 <code>greeting</code> 值的真假删除/插入 <code>&lt;p&gt;</code> 元素。</p> <h3 id=->参数</h3> <p>有些指令可以在其名称后面带一个“参数” (Argument)，中间放一个冒号隔开。例如，<code>v-bind</code> 指令用于响应地更新 HTML 特性：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>a</span> <span class=attr>v-bind:href</span>=<span class=string>"url"</span>&gt;</span><span class=tag>&lt;/<span class=name>a</span>&gt;</span></div></pre></td></tr></table></figure><p>这里 <code>href</code> 是参数，它告诉 <code>v-bind</code> 指令将元素的 <code>href</code> 特性跟表达式 <code>url</code> 的值绑定。可能你已注意到可以用特性插值 <code>href=&quot;{{url}}&quot;</code> 获得同样的结果：这样没错，并且实际上在内部特性插值会转为 <code>v-bind</code> 绑定。</p> <p>另一个例子是 <code>v-on</code> 指令，它用于监听 DOM 事件：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>a</span> <span class=attr>v-on:click</span>=<span class=string>"doSomething"</span>&gt;</span></div></pre></td></tr></table></figure><p>这里参数是被监听的事件的名字。我们也会详细说明事件绑定。</p> <h3 id=->修饰符</h3> <p>修饰符 (Modifiers) 是以半角句号 <code>.</code> 开始的特殊后缀，用于表示指令应当以特殊方式绑定。例如 <code>.literal</code> 修饰符告诉指令将它的值解析为一个字面字符串而不是一个表达式：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>a</span> <span class=attr>v-bind:href.literal</span>=<span class=string>"/a/b/c"</span>&gt;</span><span class=tag>&lt;/<span class=name>a</span>&gt;</span></div></pre></td></tr></table></figure><p>当然，这似乎没有意义，因为我们只需要使用 <code>href=&quot;/a/b/c&quot;</code> 而不必使用一个指令。这个例子只是为了演示语法。后面我们将看到修饰符更多的实践用法。</p> <h2 id=->缩写</h2> <p><code>v-</code> 前缀是一种标识模板中特定的 Vue 特性的视觉暗示。当你需要在一些现有的 HTML 代码中添加动态行为时，这些前缀可以起到很好的区分效果。但你在使用一些常用指令的时候，你会感觉一直这么写实在是啰嗦。而且在构建单页应用（<a href=https://en.wikipedia.org/wiki/Single-page_application>SPA</a> ）时，Vue.js 会管理所有的模板，此时 <code>v-</code> 前缀也没那么重要了。因此Vue.js 为两个最常用的指令 <code>v-bind</code> 和 <code>v-on</code> 提供特别的缩写：</p> <h3 id=-v-bind-><code>v-bind</code> 缩写</h3> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=comment>&lt;!-- 完整语法 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>a</span> <span class=attr>v-bind:href</span>=<span class=string>"url"</span>&gt;</span><span class=tag>&lt;/<span class=name>a</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 缩写 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>a</span> <span class=attr>:href</span>=<span class=string>"url"</span>&gt;</span><span class=tag>&lt;/<span class=name>a</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 完整语法 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>button</span> <span class=attr>v-bind:disabled</span>=<span class=string>"someDynamicCondition"</span>&gt;</span>Button<span class=tag>&lt;/<span class=name>button</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 缩写 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>button</span> <span class=attr>:disabled</span>=<span class=string>"someDynamicCondition"</span>&gt;</span>Button<span class=tag>&lt;/<span class=name>button</span>&gt;</span></div></pre></td></tr></table></figure><h3 id=-v-on-><code>v-on</code> 缩写</h3> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=comment>&lt;!-- 完整语法 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>a</span> <span class=attr>v-on:click</span>=<span class=string>"doSomething"</span>&gt;</span><span class=tag>&lt;/<span class=name>a</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 缩写 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>a</span> @<span class=attr>click</span>=<span class=string>"doSomething"</span>&gt;</span><span class=tag>&lt;/<span class=name>a</span>&gt;</span></div></pre></td></tr></table></figure><p>它们看起来跟“合法”的 HTML 有点不同，但是它们在所有 Vue.js 支持的浏览器中都能被正确地解析，并且不会出现在最终渲染的标记中。缩写语法完全是可选的，不过随着一步步学习的深入，你会庆幸拥有它们。 </p>'}});