webpackJsonp([35],{35:function(s,a,l){var n,i;__vdoc_config_=l(79),i=l(107),s.exports=n||{},s.exports.__esModule&&(s.exports=s.exports["default"]),i&&(("function"==typeof s.exports?s.exports.options:s.exports).html=i)},79:function(s,a){},107:function(s,a){s.exports='<h2 id=->全局配置</h2> <p><code>ue.config</code> 是一个对象，包含 Vue 的全局配置。可以在启动应用之前修改下面属性：</p> <h3 id=debug>debug</h3> <ul> <li><p><strong>类型：</strong> <code>Boolean</code></p> </li> <li><p><strong>默认值：</strong> <code>false</code></p> </li> <li><p><strong>用法：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line>Vue.config.debug = <span class=literal>true</span></div></pre></td></tr></table></figure><p>在调试模式中，Vue 会：</p> <ol> <li><p>为所有的警告打印栈追踪。</p> </li> <li><p>把所有的锚节点以注释节点显示在 DOM 中，更易于检查渲染结果的结构。</p> </li> </ol> </li> </ul> <p class=tip>只有开发版本可以使用调试模式。</p> <h3 id=delimiters>delimiters</h3> <ul> <li><p><strong>类型：</strong> <code>Array&lt;String&gt;</code></p> </li> <li><p><strong>默认值：</strong> <code>[&quot;{{&quot;, &quot;}}&quot;]</code></p> </li> <li><p><strong>用法：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// ES6 模板字符串</span></div><div class=line>Vue.config.delimiters = [<span class=string>\'${\'</span>, <span class=string>\'}\'</span>]</div></pre></td></tr></table></figure><p>修改文本插值的定界符。</p> </li> </ul> <h3 id=unsafedelimiters>unsafeDelimiters</h3> <ul> <li><p><strong>类型：</strong> <code>Array&lt;String&gt;</code></p> </li> <li><p><strong>默认值：</strong> <code>[&quot;{{{&quot;, &quot;}}}&quot;]</code></p> </li> <li><p><strong>用法：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// make it look more dangerous</span></div><div class=line>Vue.config.unsafeDelimiters = [<span class=string>\'{!!\'</span>, <span class=string>\'!!}\'</span>]</div></pre></td></tr></table></figure><p>修改原生 HTML 插值的定界符。</p> </li> </ul> <h3 id=silent>silent</h3> <ul> <li><p><strong>类型：</strong> <code>Boolean</code></p> </li> <li><p><strong>默认值：</strong> <code>false</code></p> </li> <li><p><strong>用法：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line>Vue.config.silent = <span class=literal>true</span></div></pre></td></tr></table></figure><p>取消 Vue.js 所有的日志与警告。</p> </li> </ul> <h3 id=async>async</h3> <ul> <li><p><strong>类型：</strong> <code>Boolean</code></p> </li> <li><p><strong>默认值：</strong> <code>true</code></p> </li> <li><p><strong>用法：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line>Vue.config.async = <span class=literal>false</span></div></pre></td></tr></table></figure><p>如果关闭了异步模式，Vue 在检测到数据变化时同步更新 DOM。在有些情况下这有助于调试，但是也可能导致性能下降，并且影响 watcher 回调的调用顺序。<strong><code>async: false</code>不推荐用在生产环境中。</strong></p> </li> </ul> <h3 id=devtools>devtools</h3> <ul> <li><p><strong>类型：</strong> <code>Boolean</code></p> </li> <li><p><strong>默认值：</strong> <code>true</code> (生产版为 <code>false</code>)</p> </li> <li><p><strong>用法：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 在加载 Vue 之后立即同步的设置</span></div><div class=line>Vue.config.devtools = <span class=literal>true</span></div></pre></td></tr></table></figure><p>配置是否允许 <a href=https://github.com/vuejs/vue-devtools>vue-devtools</a> 检查代码。开发版默认为 <code>true</code>， 生产版默认为 <code>false</code>。 生产版设为 <code>true</code> 可以启用检查。</p> </li> </ul> <h2 id=-api>全局 API</h2> <h3 id=Vue-extend>Vue.extend( options )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Object} options</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>创建基础 Vue 构造器的“子类”。参数是一个对象，包含组件选项。</p> <p>这里要注意的特例是 <code>el</code> 和 <code>data</code> 选项—— 在 <code>Vue.extend()</code> 中它们必须是函数。</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>id</span>=<span class=string>"mount-point"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 创建可复用的构造器</span></div><div class=line><span class=keyword>var</span> Profile = Vue.extend({</div><div class=line>  template: <span class=string>\'&lt;p&gt;{{firstName}} {{lastName}} aka {{alias}}&lt;/p&gt;\'</span></div><div class=line>})</div><div class=line><span class=comment>// 创建一个 Profile 实例</span></div><div class=line><span class=keyword>var</span> profile = <span class=keyword>new</span> Profile({</div><div class=line>  data: {</div><div class=line>    firstName: <span class=string>\'Walter\'</span>,</div><div class=line>    lastName: <span class=string>\'White\'</span>,</div><div class=line>    alias: <span class=string>\'Heisenberg\'</span></div><div class=line>  }</div><div class=line>})</div><div class=line><span class=comment>// 挂载到元素上</span></div><div class=line>profile.$mount(<span class=string>\'#mount-point\'</span>)</div></pre></td></tr></table></figure><p>结果：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>p</span>&gt;</span>Walter White aka Heisenberg<span class=tag>&lt;/<span class=name>p</span>&gt;</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/components.html>组件</a></p> </li> </ul> <h3 id=Vue-nextTick>Vue.nextTick( callback )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Function} callback</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>延迟回调在下次 DOM 更新循环之后执行。在修改数据之后立即使用这个方法，等待 DOM 更新。</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 修改数据</span></div><div class=line>vm.msg = <span class=string>\'Hello\'</span></div><div class=line><span class=comment>// DOM 没有更新</span></div><div class=line>Vue.nextTick(<span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>  <span class=comment>// DOM 更新了</span></div><div class=line>})</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/reactivity.html#异步更新队列>异步更新队列</a></p> </li> </ul> <h3 id=Vue-set>Vue.set( object, key, value )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Object} object</code></li> <li><code>{String} key</code></li> <li><code>{*} value</code></li> </ul> </li> <li><p><strong>返回值：</strong> 设置的值</p> </li> <li><p><strong>用法：</strong></p> <p>设置对象的属性。如果对象是响应的，将触发视图更新。这个方法主要用于解决 不能检测到属性添加的限制。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/reactivity.html>深入响应</a></p> </li> </ul> <h3 id=Vue-delete>Vue.delete( object, key )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Object} object</code></li> <li><code>{String} key</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>删除对象的属性。如果对象是响应的，将触发视图更新。这个方法主要用于解决 不能检测到属性删除的限制。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/reactivity.html>深入响应</a></p> </li> </ul> <h3 id=Vue-directive>Vue.directive( id, [definition] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} id</code></li> <li><code>{Function | Object} [definition]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>注册或获取全局指令。</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 注册</span></div><div class=line>Vue.directive(<span class=string>\'my-directive\'</span>, {</div><div class=line>  bind: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{},</div><div class=line>  update: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{},</div><div class=line>  unbind: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{}</div><div class=line>})</div><div class=line></div><div class=line><span class=comment>// 注册，传入一个函数</span></div><div class=line>Vue.directive(<span class=string>\'my-directive\'</span>, <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>  <span class=comment>// this will be called as `update`</span></div><div class=line>})</div><div class=line></div><div class=line><span class=comment>// getter，返回已注册的指令</span></div><div class=line><span class=keyword>var</span> myDirective = Vue.directive(<span class=string>\'my-directive\'</span>)</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/custom-directive.html>自定义指令</a></p> </li> </ul> <h3 id=Vue-elementDirective>Vue.elementDirective( id, [definition] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} id</code></li> <li><code>{Object} [definition]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>注册或获取全局的元素指令。</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 注册</span></div><div class=line>Vue.elementDirective(<span class=string>\'my-element\'</span>, {</div><div class=line>  bind: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{},</div><div class=line>  <span class=comment>// 没有使用 `update`</span></div><div class=line>  unbind: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{}</div><div class=line>})</div><div class=line></div><div class=line><span class=comment>// getter，返回已注册的元素指令</span></div><div class=line><span class=keyword>var</span> myDirective = Vue.elementDirective(<span class=string>\'my-element\'</span>)</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/custom-directive.html#元素指令>元素指令</a></p> </li> </ul> <h3 id=Vue-filter>Vue.filter( id, [definition] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} id</code></li> <li><code>{Function | Object} [definition]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>注册或获取全局过滤器。</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 注册</span></div><div class=line>Vue.filter(<span class=string>\'my-filter\'</span>, <span class=function><span class=keyword>function</span> (<span class=params>value</span>) </span>{</div><div class=line>  <span class=comment>// 返回处理后的值</span></div><div class=line>})</div><div class=line></div><div class=line><span class=comment>// 双向过滤器</span></div><div class=line>Vue.filter(<span class=string>\'my-filter\'</span>, {</div><div class=line>  read: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{},</div><div class=line>  write: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{}</div><div class=line>})</div><div class=line></div><div class=line><span class=comment>// getter，返回已注册的指令</span></div><div class=line><span class=keyword>var</span> myFilter = Vue.filter(<span class=string>\'my-filter\'</span>)</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/custom-filter.html>自定义过滤器</a></p> </li> </ul> <h3 id=Vue-component>Vue.component( id, [definition] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} id</code></li> <li><code>{Function | Object} [definition]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>注册或获取全局组件。</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 注册组件，传入一个扩展的构造器</span></div><div class=line>Vue.component(<span class=string>\'my-component\'</span>, Vue.extend({ <span class=comment>/* ... */</span>}))</div><div class=line></div><div class=line><span class=comment>// 注册组件，传入一个选项对象（自动调用 Vue.extend）</span></div><div class=line>Vue.component(<span class=string>\'my-component\'</span>, { <span class=comment>/* ... */</span> })</div><div class=line></div><div class=line><span class=comment>// 获取注册的组件（始终返回构造器）</span></div><div class=line><span class=keyword>var</span> MyComponent = Vue.component(<span class=string>\'my-component\'</span>)</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/components.html>组件</a>.</p> </li> </ul> <h3 id=Vue-transition>Vue.transition( id, [hooks] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} id</code></li> <li><code>{Object} [hooks]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>注册或获取全局的过渡钩子对象。</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 注册</span></div><div class=line>Vue.transition(<span class=string>\'fade\'</span>, {</div><div class=line>  enter: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{},</div><div class=line>  leave: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{}</div><div class=line>})</div><div class=line></div><div class=line><span class=comment>// 获取注册的钩子</span></div><div class=line><span class=keyword>var</span> fadeTransition = Vue.transition(<span class=string>\'fade\'</span>)</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/transitions.html>过渡</a>.</p> </li> </ul> <h3 id=Vue-partial>Vue.partial( id, [partial] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} id</code></li> <li><code>{String} [partial]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>注册或获取全局的 partial。</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 注册</span></div><div class=line>Vue.partial(<span class=string>\'my-partial\'</span>, <span class=string>\'&lt;div&gt;Hi&lt;/div&gt;\'</span>)</div><div class=line></div><div class=line><span class=comment>// 获取注册的 partial</span></div><div class=line><span class=keyword>var</span> myPartial = Vue.partial(<span class=string>\'my-partial\'</span>)</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=#partial>特殊元素 - &lt;partial&gt;</a>。</p> </li> </ul> <h3 id=Vue-use>Vue.use( plugin, [options] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Object | Function} plugin</code></li> <li><code>{Object} [options]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>安装 Vue.js 插件。如果插件是一个对象，必须有一个 <code>install</code> 方法。如果它是一个函数，它会被作为安装方法。安装方法以 Vue 为参数。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/plugins.html>插件</a>.</p> </li> </ul> <h3 id=Vue-mixin>Vue.mixin( mixin )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Object} mixin</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>全局应用一个混合，将影响所有 Vue 实例。插件作者可以用它向组件注入自定义逻辑。<strong>不推荐用在应用代码中。</strong></p> </li> <li><p><strong>另见：</strong> <a href=/guide/mixins.html#全局混合>全局混合</a></p> </li> </ul> <h2 id=->选项 / 数据</h2> <h3 id=data>data</h3> <ul> <li><p><strong>类型：</strong> <code>Object | Function</code></p> </li> <li><p><strong>限制：</strong> 在组件定义中只能是函数。</p> </li> <li><p><strong>详细：</strong></p> <p>Vue 实例的数据对象。Vue.js 会递归地将它全部属性转为 getter/setter，从而让它能响应数据变化。<strong>这个对象必须是普通对象</strong>：原生对象，getter/setter 及原型属性会被忽略。不推荐观察复杂对象。</p> <p>在实例创建之后，可以用 <code>vm.$data</code> 访问原始数据对象。Vue 实例也代理了数据对象所有的属性。</p> <p>在定义<strong>组件</strong>时，同一定义将创建多个实例，此时 <code>data</code> 必须是一个函数，返回原始数据对象。如果 <code>data</code> 仍然是一个普通对象，则所有的实例将指向同一个对象！换成函数后，每当创建一个实例时，会调用这个函数，返回一个新的原始数据对象的副本。</p> <p>名字以 <code>_</code> 或 <code>$</code>开始的属性<strong>不会</strong>被 Vue 实例代理，因为它们可能与 Vue 的内置属性与 API 方法冲突。用 <code>vm.$data._property</code> 访问它们。</p> <p>可以通过将 <code>vm.$data</code> 传入 <code>JSON.parse(JSON.stringify(...))</code> 得到原始数据对象。</p> </li> </ul> <ul> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> data = { a: <span class=number>1</span> }</div><div class=line></div><div class=line><span class=comment>// 直接创建一个实例</span></div><div class=line><span class=keyword>var</span> vm = <span class=keyword>new</span> Vue({</div><div class=line>  data: data</div><div class=line>})</div><div class=line>vm.a <span class=comment>// -&gt; 1</span></div><div class=line>vm.$data === data <span class=comment>// -&gt; true</span></div><div class=line></div><div class=line><span class=comment>// 在 Vue.extend() 中必须是函数</span></div><div class=line><span class=keyword>var</span> Component = Vue.extend({</div><div class=line>  data: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>    <span class=keyword>return</span> { a: <span class=number>1</span> }</div><div class=line>  }</div><div class=line>})</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/reactivity.html>深入响应</a>.</p> </li> </ul> <h3 id=props>props</h3> <ul> <li><p><strong>类型：</strong> <code>Array | Object</code></p> </li> <li><p><strong>详细：</strong></p> <p>包含一些特性——期望使用的父组件数据的属性。可以是数组或对象。对象用于高级配置，如类型检查，自定义验证，默认值等。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 简单语法</span></div><div class=line>Vue.component(<span class=string>\'props-demo-simple\'</span>, {</div><div class=line>  props: [<span class=string>\'size\'</span>, <span class=string>\'myMessage\'</span>]</div><div class=line>})</div><div class=line></div><div class=line><span class=comment>// 对象语法，指定验证要求</span></div><div class=line>Vue.component(<span class=string>\'props-demo-advanced\'</span>, {</div><div class=line>  props: {</div><div class=line>    <span class=comment>// 只检测类型</span></div><div class=line>    size: <span class=built_in>Number</span>,</div><div class=line>    <span class=comment>// 检测类型 + 其它验证</span></div><div class=line>    name: {</div><div class=line>      type: <span class=built_in>String</span>,</div><div class=line>      required: <span class=literal>true</span></div><div class=line>    }</div><div class=line>  }</div><div class=line>})</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/components.html#Props>Props</a></p> </li> </ul> <h3 id=computed>computed</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>详细：</strong></p> <p>实例计算属性。getter 和 setter 的 <code>this</code> 自动地绑定到实例。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> vm = <span class=keyword>new</span> Vue({</div><div class=line>  data: { a: <span class=number>1</span> },</div><div class=line>  computed: {</div><div class=line>    <span class=comment>// 仅读取，值只须为函数</span></div><div class=line>    aDouble: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>      <span class=keyword>return</span> <span class=keyword>this</span>.a * <span class=number>2</span></div><div class=line>    },</div><div class=line>    <span class=comment>// 读取和设置</span></div><div class=line>    aPlus: {</div><div class=line>      get: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>        <span class=keyword>return</span> <span class=keyword>this</span>.a + <span class=number>1</span></div><div class=line>      },</div><div class=line>      set: <span class=function><span class=keyword>function</span> (<span class=params>v</span>) </span>{</div><div class=line>        <span class=keyword>this</span>.a = v - <span class=number>1</span></div><div class=line>      }</div><div class=line>    }</div><div class=line>  }</div><div class=line>})</div><div class=line>vm.aPlus   <span class=comment>// -&gt; 2</span></div><div class=line>vm.aPlus = <span class=number>3</span></div><div class=line>vm.a       <span class=comment>// -&gt; 2</span></div><div class=line>vm.aDouble <span class=comment>// -&gt; 4</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong></p> <ul> <li><a href=/guide/computed.html>计算属性</a></li> <li><a href=/guide/reactivity.html#计算属性>深入响应：计算属性</a></li> </ul> </li> </ul> <h3 id=methods>methods</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>详细：</strong></p> <p>实例方法。实例可以直接访问这些方法，也可以用在指令表达式内。方法的 <code>this</code> 自动绑定到实例。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> vm = <span class=keyword>new</span> Vue({</div><div class=line>  data: { a: <span class=number>1</span> },</div><div class=line>  methods: {</div><div class=line>    plus: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>      <span class=keyword>this</span>.a++</div><div class=line>    }</div><div class=line>  }</div><div class=line>})</div><div class=line>vm.plus()</div><div class=line>vm.a <span class=comment>// 2</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/events.html>方法与事件处理器</a></p> </li> </ul> <h3 id=watch>watch</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>详细：</strong></p> <p>一个对象，键是观察表达式，值是对应回调。值也可以是方法名，或者是对象，包含选项。在实例化时为每个键调用 <code>$watch()</code> 。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> vm = <span class=keyword>new</span> Vue({</div><div class=line>  data: {</div><div class=line>    a: <span class=number>1</span></div><div class=line>  },</div><div class=line>  watch: {</div><div class=line>    <span class=string>\'a\'</span>: <span class=function><span class=keyword>function</span> (<span class=params>val, oldVal</span>) </span>{</div><div class=line>      <span class=built_in>console</span>.log(<span class=string>\'new: %s, old: %s\'</span>, val, oldVal)</div><div class=line>    },</div><div class=line>    <span class=comment>// 方法名</span></div><div class=line>    <span class=string>\'b\'</span>: <span class=string>\'someMethod\'</span>,</div><div class=line>    <span class=comment>// 深度 watcher</span></div><div class=line>    <span class=string>\'c\'</span>: {</div><div class=line>      handler: <span class=function><span class=keyword>function</span> (<span class=params>val, oldVal</span>) </span>{ <span class=comment>/* ... */</span> },</div><div class=line>      deep: <span class=literal>true</span></div><div class=line>    }</div><div class=line>  }</div><div class=line>})</div><div class=line>vm.a = <span class=number>2</span> <span class=comment>// -&gt; new: 2, old: 1</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=#vm-watch>实例方法 - vm.$watch</a></p> </li> </ul> <h2 id=-dom>选项 / DOM</h2> <h3 id=el>el</h3> <ul> <li><p><strong>类型：</strong> <code>String | HTMLElement | Function</code></p> </li> <li><p><strong>限制：</strong> 在组件定义中只能是函数。</p> </li> <li><p><strong>详细：</strong></p> <p>为实例提供挂载元素。值可以是 CSS 选择符，或实际 HTML 元素，或返回 HTML 元素的函数。注意元素只用作挂载点。如果提供了模板则元素被替换，除非 <code>replace</code> 为 false。元素可以用 <code>vm.$el</code> 访问。</p> <p>用在 <code>Vue.extend</code> 中必须是函数值，这样所有实例不会共享元素。</p> <p>如果在初始化时指定了这个选项，实例将立即进入编译过程。否则，需要调用 <code>vm.$mount()</code>，手动开始编译。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/instance.html#生命周期图示>生命周期图示</a></p> </li> </ul> <h3 id=template>template</h3> <ul> <li><p><strong>类型：</strong> <code>String</code></p> </li> <li><p><strong>详细：</strong></p> <p>实例模板。模板默认<strong>替换</strong>挂载元素。如果 <code>replace</code> 选项为 <code>false</code>，模板将插入挂载元素内。两种情况下，挂载元素的内容都将被忽略，除非模板有内容分发 slot。</p> <p>如果值以 <code>#</code> 开始，则它用作选项符，将使用匹配元素的 innerHTML 作为模板。常用的技巧是用 <code>&lt;script type=&quot;x-template&quot;&gt;</code> 包含模板。</p> <p>注意在一些情况下，例如如模板包含多个顶级元素，或只包含普通文本，实例将变成一个片断实例，管理多个节点而不是一个节点。片断实例的挂载元素上的非流程控制指令被忽略。</p> </li> <li><p><strong>另见：</strong></p> <ul> <li><a href=/guide/instance.html#生命周期图示>生命周期图示</a></li> <li><a href=/guide/components.html#使用-Slot-分发内容>使用 slot 分发内容</a></li> <li><a href=/guide/components.html#片断实例>片断实例</a></li> </ul> </li> </ul> <h3 id=replace>replace</h3> <ul> <li><p><strong>类型：</strong> <code>Boolean</code></p> </li> <li><p><strong>默认值：</strong> <code>true</code></p> </li> <li><p><strong>限制：</strong> 只能与 <strong>template</strong> 选项一起用</p> </li> <li><p><strong>详细：</strong></p> <p>决定是否替换模板的挂载元素。如果设为 <code>false</code> 模板将覆盖元素的内容，不会替换元素本身。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>id</span>=<span class=string>"replace"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>new</span> Vue({</div><div class=line>  el: <span class=string>\'#replace\'</span>,</div><div class=line>  template: <span class=string>\'&lt;p&gt;replaced&lt;/p&gt;\'</span></div><div class=line>})</div></pre></td></tr></table></figure><p>结果：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>p</span>&gt;</span>replaced<span class=tag>&lt;/<span class=name>p</span>&gt;</span></div></pre></td></tr></table></figure><p><code>replace</code> 设为 <code>false</code>：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>id</span>=<span class=string>"insert"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>new</span> Vue({</div><div class=line>  el: <span class=string>\'#insert\'</span>,</div><div class=line>  replace: <span class=literal>false</span>,</div><div class=line>  template: <span class=string>\'&lt;p&gt;inserted&lt;/p&gt;\'</span></div><div class=line>})</div></pre></td></tr></table></figure><p>结果：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>id</span>=<span class=string>"insert"</span>&gt;</span></div><div class=line>  <span class=tag>&lt;<span class=name>p</span>&gt;</span>inserted<span class=tag>&lt;/<span class=name>p</span>&gt;</span></div><div class=line><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure></li> </ul> <h2 id=->选项 / 生命周期钩子</h2> <h3 id=init>init</h3> <ul> <li><p><strong>类型:</strong> <code>Function</code></p> </li> <li><p><strong>详细:</strong></p> <p>在实例开始初始化时同步调用。此时数据观测、事件和 watcher 都尚未初始化。</p> </li> <li><p><strong>另见:</strong> <a href=/guide/instance.html#生命周期图示>生命周期图示</a></p> </li> </ul> <h3 id=created>created</h3> <ul> <li><p><strong>类型：</strong> <code>Function</code></p> </li> <li><p><strong>详细：</strong></p> <p>在实例创建之后同步调用。此时实例已经结束解析选项，这意味着已建立：数据绑定，计算属性，方法，watcher/事件回调。但是还没有开始 DOM 编译，<code>$el</code> 还不存在。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/instance.html#生命周期图示>生命周期图示</a></p> </li> </ul> <h3 id=beforecompile>beforeCompile</h3> <ul> <li><p><strong>类型：</strong> <code>Function</code></p> </li> <li><p><strong>详细：</strong></p> <p>在编译开始前调用。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/instance.html#生命周期图示>生命周期图示</a></p> </li> </ul> <h3 id=compiled>compiled</h3> <ul> <li><p><strong>类型：</strong> <code>Function</code></p> </li> <li><p><strong>详细：</strong></p> <p>在编译结束后调用。此时所有的指令已生效，因而数据的变化将触发 DOM 更新。但是不担保 <code>$el</code> 已插入文档。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/instance.html#生命周期图示>生命周期图示</a></p> </li> </ul> <h3 id=ready>ready</h3> <ul> <li><p><strong>类型：</strong> <code>Function</code></p> </li> <li><p><strong>详细：</strong></p> <p>在编译结束和<strong> <code>$el</code> 第一次插入文档</strong>之后调用，如在第一次 <code>attached</code> 钩子之后调用。注意必须是由 Vue 插入（如 <code>vm.$appendTo()</code> 等方法或指令更新）才触发 <code>ready</code> 钩子。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/instance.html#生命周期图示>生命周期图示</a></p> </li> </ul> <h3 id=attached>attached</h3> <ul> <li><p><strong>类型：</strong> <code>Function</code></p> </li> <li><p><strong>详细：</strong></p> <p>在 <code>vm.$el</code> 插入 DOM 时调用。必须是由指令或实例方法（如 <code>$appendTo()</code>）插入，直接操作 <code>vm.$el</code> <strong>不会</strong> 触发这个钩子。</p> </li> </ul> <h3 id=detached>detached</h3> <ul> <li><p><strong>类型：</strong> <code>Function</code></p> </li> <li><p><strong>详细：</strong></p> <p>在 <code>vm.$el</code> 从 DOM 中删除时调用。必须是由指令或实例方法删除，直接操作 <code>vm.$el</code> <strong>不会</strong> 触发这个钩子。</p> </li> </ul> <h3 id=beforedestroy>beforeDestroy</h3> <ul> <li><p><strong>类型：</strong> <code>Function</code></p> </li> <li><p><strong>详细：</strong></p> <p>在开始销毁实例时调用。此时实例仍然有功能。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/instance.html#生命周期图示>生命周期图示</a></p> </li> </ul> <h3 id=destroyed>destroyed</h3> <ul> <li><p><strong>类型：</strong> <code>Function</code></p> </li> <li><p><strong>详细：</strong></p> <p>在实例被销毁之后调用。此时所有的绑定和实例的指令已经解绑，所有的子实例也已经被销毁。</p> <p>如果有离开过渡，<code>destroyed</code> 钩子在过渡完成<strong>之后</strong>调用。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/instance.html#生命周期图示>生命周期图示</a></p> </li> </ul> <h2 id=->选项 / 资源</h2> <h3 id=directives>directives</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>详细：</strong></p> <p>一个对象，包含指令。</p> </li> <li><p><strong>另见：</strong></p> <ul> <li><a href=/guide/custom-directive.html>自定义指令</a></li> <li><a href=/guide/components.html#资源命名约定>资源命名约定</a></li> </ul> </li> </ul> <h3 id=elementdirectives>elementDirectives</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>详细：</strong></p> <p>一个对象，包含元素指令。</p> </li> <li><p><strong>另见：</strong></p> <ul> <li><a href=/guide/custom-directive.html#Element-Directives>元素指令</a></li> <li><a href=/guide/components.html#资源命名约定>资源命名约定</a></li> </ul> </li> </ul> <h3 id=filters>filters</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>详细：</strong></p> <p>一个对象，包含过滤器。</p> </li> <li><p><strong>另见：</strong></p> <ul> <li><a href=/guide/custom-filter.html>自定义过滤器</a></li> <li><a href=/guide/components.html#资源命名约定>资源命名约定</a></li> </ul> </li> </ul> <h3 id=components>components</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>详细：</strong></p> <p>一个对象，包含组件。</p> </li> <li><p><strong>另见：</strong></p> <ul> <li><a href=/guide/components.html>组件</a></li> </ul> </li> </ul> <h3 id=transitions>transitions</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>详细：</strong></p> <p>一个对象，包含过渡。</p> </li> <li><p><strong>另见：</strong></p> <ul> <li><a href=/guide/transitions.html>过渡</a></li> </ul> </li> </ul> <h3 id=partials>partials</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>详细：</strong></p> <p>一个对象，包含 partial。</p> </li> <li><p><strong>另见：</strong></p> <ul> <li><a href=#partial>特殊元素 - partial</a></li> </ul> </li> </ul> <h2 id=->选项 / 杂项</h2> <h3 id=parent>parent</h3> <ul> <li><p><strong>类型：</strong> <code>Vue 实例</code></p> </li> <li><p><strong>详细：</strong></p> <p>指定实例的父实例，在两者之间建立父子关系。子实例可以用 <code>this.$parent</code> 访问父实例，子实例被推入父实例的 <code>$children</code> 数组中。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/components.html#父子组件通信>父子组件通信</a></p> </li> </ul> <h3 id=events>events</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>详细：</strong></p> <p>一个对象，键是监听的事件，值是相应的回调。注意这些事件是 Vue 事件而不是 DOM 事件。值也可以是方法的名字。在实例化的过程中，Vue 实例会调用对象的每个键。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> vm = <span class=keyword>new</span> Vue({</div><div class=line>  events: {</div><div class=line>    <span class=string>\'hook:created\'</span>: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>      <span class=built_in>console</span>.log(<span class=string>\'created!\'</span>)</div><div class=line>    },</div><div class=line>    greeting: <span class=function><span class=keyword>function</span> (<span class=params>msg</span>) </span>{</div><div class=line>      <span class=built_in>console</span>.log(msg)</div><div class=line>    },</div><div class=line>    <span class=comment>// 也可以是方法的名字</span></div><div class=line>    bye: <span class=string>\'sayGoodbye\'</span></div><div class=line>  },</div><div class=line>  methods: {</div><div class=line>    sayGoodbye: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>      <span class=built_in>console</span>.log(<span class=string>\'goodbye!\'</span>)</div><div class=line>    }</div><div class=line>  }</div><div class=line>}) <span class=comment>// -&gt; created!</span></div><div class=line>vm.$emit(<span class=string>\'greeting\'</span>, <span class=string>\'hi!\'</span>) <span class=comment>// -&gt; hi!</span></div><div class=line>vm.$emit(<span class=string>\'bye\'</span>)             <span class=comment>// -&gt; goodbye!</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong></p> <ul> <li><a href=#Instance_Methods_/_Events>实例方法 - 事件</a></li> <li><a href=/guide/components.html#父子组件通信>父子组件通信</a></li> </ul> </li> </ul> <h3 id=mixins>mixins</h3> <ul> <li><p><strong>类型：</strong> <code>Array</code></p> </li> <li><p><strong>详细：</strong></p> <p>一个数组，包含混合对象。这些混合对象可以像普通实例对象一样包含实例选项，它们将合并成一个最终选项对象，合并策略同 <code>Vue.extend()</code>。比如，如果混合对象包含一个 <code>created</code> 钩子，组件自身也包含一个，两个钩子函数都会被调用。</p> <p>混合后的钩子按它们出现顺序调用，并且是在调用组件自己的钩子之前调用。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> mixin = {</div><div class=line>  created: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{ <span class=built_in>console</span>.log(<span class=number>1</span>) }</div><div class=line>}</div><div class=line><span class=keyword>var</span> vm = <span class=keyword>new</span> Vue({</div><div class=line>  created: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{ <span class=built_in>console</span>.log(<span class=number>2</span>) },</div><div class=line>  mixins: [mixin]</div><div class=line>})</div><div class=line><span class=comment>// -&gt; 1</span></div><div class=line><span class=comment>// -&gt; 2</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/mixins.html>混合</a></p> </li> </ul> <h3 id=name>name</h3> <ul> <li><p><strong>类型：</strong> <code>String</code></p> </li> <li><p><strong>限制：</strong> 只能用在 <code>Vue.extend()</code> 中。</p> </li> <li><p><strong>详细：</strong></p> <p>允许组件在它的模板内递归地调用它自己。注意如果组件是由 <code>Vue.component()</code> 全局注册，全局 ID 自动作为它的名字。</p> <p>指定 <code>name</code> 选项的另一个好处是方便检查。当在控制台检查组件时，默认的构造器名字是 <code>VueComponent</code> ，不大有用。在向 <code>Vue.extend()</code> 传入 <code>name</code> 选项后，可以知道正在检查哪个组件。值会被转换为驼峰形式，并用作组件构造器的名字。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> Ctor = Vue.extend({</div><div class=line>  name: <span class=string>\'stack-overflow\'</span>,</div><div class=line>  template:</div><div class=line>    <span class=string>\'&lt;div&gt;\'</span> +</div><div class=line>      <span class=comment>// 递归地调用自己</span></div><div class=line>      <span class=string>\'&lt;stack-overflow&gt;&lt;/stack-overflow&gt;\'</span> +</div><div class=line>    <span class=string>\'&lt;/div&gt;\'</span></div><div class=line>})</div><div class=line></div><div class=line><span class=comment>// 将导致错误：Maximum call stack size exceeded</span></div><div class=line><span class=comment>// 不过我们假定没问题...</span></div><div class=line><span class=keyword>var</span> vm = <span class=keyword>new</span> Ctor()</div><div class=line></div><div class=line><span class=built_in>console</span>.log(vm) <span class=comment>// -&gt; StackOverflow {$el: null, ...}</span></div></pre></td></tr></table></figure></li> </ul> <h2 id=->实例属性</h2> <h3 id=vm-data>vm.$data</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>详细：</strong></p> <p>Vue 实例观察的数据对象。可以用一个新的对象替换。实例代理了它的数据对象的属性。</p> </li> </ul> <h3 id=vm-el>vm.$el</h3> <ul> <li><p><strong>类型：</strong> <code>HTMLElement</code></p> </li> <li><p><strong>只读</strong></p> </li> <li><p><strong>详细：</strong></p> <p>Vue 实例的挂载元素。注意对于<a href=/guide/components.html#片断实例>片段实例</a>，<code>vm.$el</code> 返回一个锚节点，指示片断的开始位置。</p> </li> </ul> <h3 id=vm-options>vm.$options</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>只读</strong></p> </li> <li><p><strong>详细：</strong></p> <p>当前实例的初始化选项。在选项中包含自定义属性时有用处：</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>new</span> Vue({</div><div class=line>  customOption: <span class=string>\'foo\'</span>,</div><div class=line>  created: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>    <span class=built_in>console</span>.log(<span class=keyword>this</span>.$options.customOption) <span class=comment>// -&gt; \'foo\'</span></div><div class=line>  }</div><div class=line>})</div></pre></td></tr></table></figure></li> </ul> <h3 id=vm-parent>vm.$parent</h3> <ul> <li><p><strong>类型：</strong> <code>Vue 实例</code></p> </li> <li><p><strong>只读</strong></p> </li> <li><p><strong>详细：</strong></p> <p>父实例，如果当前实例有的话。</p> </li> </ul> <h3 id=vm-root>vm.$root</h3> <ul> <li><p><strong>类型：</strong> <code>Vue 实例</code></p> </li> <li><p><strong>只读</strong></p> </li> <li><p><strong>详细：</strong></p> <p>当前组件树的根 Vue 实例。如果当前实例没有父实例，值将是它自身。</p> </li> </ul> <h3 id=vm-children>vm.$children</h3> <ul> <li><p><strong>类型：</strong> <code>Array&lt;Vue instance&gt;</code></p> </li> <li><p><strong>只读</strong></p> </li> <li><p><strong>详细：</strong></p> <p>当前实例的直接子组件。</p> </li> </ul> <h3 id=vm-refs>vm.$refs</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>只读</strong></p> </li> <li><p><strong>详细：</strong></p> <p>一个对象，包含注册有 <code>v-ref</code> 的子组件。</p> </li> <li><p><strong>另见：</strong></p> <ul> <li><a href=/guide/components.html#子组件索引>子组件索引</a></li> <li><a href=#v-ref>v-ref</a></li> </ul> </li> </ul> <h3 id=vm-els>vm.$els</h3> <ul> <li><p><strong>类型：</strong> <code>Object</code></p> </li> <li><p><strong>只读</strong></p> </li> <li><p><strong>详细：</strong></p> <p>一个对象，包含注册有 <code>v-el</code> 的 DOM 元素。</p> </li> <li><p><strong>另见：</strong> <a href=#v-el>v-el</a>。</p> </li> </ul> <h2 id=->实例方法 / 数据</h2> <h3 id=vm-watch>vm.$watch( expOrFn, callback, [options] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String | Function} expOrFn</code></li> <li><code>{Function} callback</code></li> <li><code>{Object} [options]</code><ul> <li><code>{Boolean} deep</code></li> <li><code>{Boolean} immediate</code></li> </ul> </li> </ul> </li> <li><p><strong>返回值：</strong> <code>{Function} unwatch</code></p> </li> <li><p><strong>用法：</strong></p> <p>观察 Vue 实例的一个表达式或计算函数。回调的参数为新值和旧值。表达式可以是某个键路径或任意合法绑定表达式。</p> </li> </ul> <p class=tip>注意：在修改（不是替换）对象或数组时，旧值将与新值相同，因为它们索引同一个对象/数组。Vue 不会保留修改之前值的副本。</p> <ul> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 键路径</span></div><div class=line>vm.$watch(<span class=string>\'a.b.c\'</span>, <span class=function><span class=keyword>function</span> (<span class=params>newVal, oldVal</span>) </span>{</div><div class=line>  <span class=comment>// 做点什么</span></div><div class=line>})</div><div class=line></div><div class=line><span class=comment>// 表达式</span></div><div class=line>vm.$watch(<span class=string>\'a + b\'</span>, <span class=function><span class=keyword>function</span> (<span class=params>newVal, oldVal</span>) </span>{</div><div class=line>  <span class=comment>// 做点什么</span></div><div class=line>})</div><div class=line></div><div class=line><span class=comment>// 函数</span></div><div class=line>vm.$watch(</div><div class=line>  <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>    <span class=keyword>return</span> <span class=keyword>this</span>.a + <span class=keyword>this</span>.b</div><div class=line>  },</div><div class=line>  <span class=function><span class=keyword>function</span> (<span class=params>newVal, oldVal</span>) </span>{</div><div class=line>    <span class=comment>// 做点什么</span></div><div class=line>  }</div><div class=line>)</div></pre></td></tr></table></figure><p><code>vm.$watch</code> 返回一个取消观察函数，用来停止触发回调：</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> unwatch = vm.$watch(<span class=string>\'a\'</span>, cb)</div><div class=line><span class=comment>// 之后取消观察</span></div><div class=line>unwatch()</div></pre></td></tr></table></figure></li> <li><p><strong>Option: deep</strong></p> <p>为了发现对象内部值的变化，可以在选项参数中指定 <code>deep: true</code>。注意监听数组的变动不需要这么做。</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line>vm.$watch(<span class=string>\'someObject\'</span>, callback, {</div><div class=line>  deep: <span class=literal>true</span></div><div class=line>})</div><div class=line>vm.someObject.nestedValue = <span class=number>123</span></div><div class=line><span class=comment>// 触发回调</span></div></pre></td></tr></table></figure></li> <li><p><strong>Option: immediate</strong></p> <p>在选项参数中指定 <code>immediate: true</code> 将立即以表达式的当前值触发回调：</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line>vm.$watch(<span class=string>\'a\'</span>, callback, {</div><div class=line>  immediate: <span class=literal>true</span></div><div class=line>})</div><div class=line><span class=comment>// 立即以 `a` 的当前值触发回调</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=vm-get>vm.$get( expression )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} expression</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>从 Vue 实例获取指定表达式的值。如果表达式抛出错误，则取消错误并返回 <code>undefined</code>。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> vm = <span class=keyword>new</span> Vue({</div><div class=line>  data: {</div><div class=line>    a: {</div><div class=line>      b: <span class=number>1</span></div><div class=line>    }</div><div class=line>  }</div><div class=line>})</div><div class=line>vm.$get(<span class=string>\'a.b\'</span>) <span class=comment>// -&gt; 1</span></div><div class=line>vm.$get(<span class=string>\'a.b + 1\'</span>) <span class=comment>// -&gt; 2</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=vm-set>vm.$set( keypath, value )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} keypath</code></li> <li><code>{*} value</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>设置 Vue 实例的属性值。多数情况下应当使用普通对象语法，如 <code>vm.a.b = 123</code>。这个方法只用于下面情况：</p> <ol> <li><p>使用 keypath 动态地设置属性。</p> </li> <li><p>设置不存在的属性。</p> </li> </ol> <p>如果 keypath 不存在，将递归地创建并建立追踪。如果用它创建一个顶级属性，实例将被强制进入“ digest 循环”，在此过程中重新计算所有的 watcher。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> vm = <span class=keyword>new</span> Vue({</div><div class=line>  data: {</div><div class=line>    a: {</div><div class=line>      b: <span class=number>1</span></div><div class=line>    }</div><div class=line>  }</div><div class=line>})</div><div class=line></div><div class=line><span class=comment>// keypath 存在</span></div><div class=line>vm.$set(<span class=string>\'a.b\'</span>, <span class=number>2</span>)</div><div class=line>vm.a.b <span class=comment>// -&gt; 2</span></div><div class=line></div><div class=line><span class=comment>// keypath 不存在</span></div><div class=line>vm.$set(<span class=string>\'c\'</span>, <span class=number>3</span>)</div><div class=line>vm.c <span class=comment>// -&gt;</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/reactivity.html>深入响应</a></p> </li> </ul> <h3 id=vm-delete>vm.$delete( key )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} key</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>删除 Vue 实例（以及它的 <code>$data</code>）上的顶级属性。强制 digest 循环，不推荐使用。</p> </li> </ul> <h3 id=vm-eval>vm.$eval( expression )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} expression</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>计算当前实例上的合法的绑定表达式。表达式也可以包含过滤器。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 假定 vm.msg = \'hello\'</span></div><div class=line>vm.$<span class=built_in>eval</span>(<span class=string>\'msg | uppercase\'</span>) <span class=comment>// -&gt; \'HELLO\'</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=vm-interpolate>vm.$interpolate( templateString )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} templateString</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>计算模板，模板包含 Mustache 标签。注意这个方法只是简单计算插值，模板内的指令将被忽略。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 假定 vm.msg = \'hello\'</span></div><div class=line>vm.$interpolate(<span class=string>\'{{msg}} world!\'</span>) <span class=comment>// -&gt; \'hello world!\'</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=vm-log>vm.$log( [keypath] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} [keypath]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>打印当前实例的数据，比起一堆 getter/setter 要友好。keypath 可选。</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line>vm.$log() <span class=comment>// 打印整个 ViewModel 的数据</span></div><div class=line>vm.$log(<span class=string>\'item\'</span>) <span class=comment>// 打印 vm.item</span></div></pre></td></tr></table></figure></li> </ul> <h2 id=->实例属性 / 事件</h2> <h3 id=vm-on>vm.$on( event, callback )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} event</code></li> <li><code>{Function} callback</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>监听当前实例上的自定义事件。事件可以由 <code>vm.$emit</code>, <code>vm.$dispatch</code> 或 <code>vm.$broadcast</code>触发。传入这些方法的附加参数都会传入这个方法的回调。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line>vm.$on(<span class=string>\'test\'</span>, <span class=function><span class=keyword>function</span> (<span class=params>msg</span>) </span>{</div><div class=line>  <span class=built_in>console</span>.log(msg)</div><div class=line>})</div><div class=line>vm.$emit(<span class=string>\'test\'</span>, <span class=string>\'hi\'</span>)</div><div class=line><span class=comment>// -&gt; "hi"</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=vm-once>vm.$once( event, callback )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} event</code></li> <li><code>{Function} callback</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>监听一个自定义事件，但是只触发一次，在第一次触发之后删除监听器。</p> </li> </ul> <h3 id=vm-off>vm.$off( [event, callback] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} [event]</code></li> <li><code>{Function} [callback]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>删除事件监听器。</p> <ul> <li><p>如果没有参数，则删除所有的事件监听器；</p> </li> <li><p>如果只提供了事件，则删除这个事件所有的监听器；</p> </li> <li><p>如果同时提供了事件与回调，则只删除这个回调。</p> </li> </ul> </li> </ul> <h3 id=vm-emit>vm.$emit( event, [...args] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} event</code></li> <li><code>[...args]</code></li> </ul> <p>触发当前实例上的事件。附加参数都会传给监听器回调。</p> </li> </ul> <h3 id=vm-dispatch>vm.$dispatch( event, [...args] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} event</code></li> <li><code>[...args]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>派发事件，首先在实例上触发它，然后沿着父链向上冒泡在触发一个监听器后停止，除非它返回 <code>true</code>。附加参数都会传给监听器回调。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 创建父链</span></div><div class=line><span class=keyword>var</span> parent = <span class=keyword>new</span> Vue()</div><div class=line><span class=keyword>var</span> child1 = <span class=keyword>new</span> Vue({ parent: parent })</div><div class=line><span class=keyword>var</span> child2 = <span class=keyword>new</span> Vue({ parent: child1 })</div><div class=line></div><div class=line>parent.$on(<span class=string>\'test\'</span>, <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>  <span class=built_in>console</span>.log(<span class=string>\'parent notified\'</span>)</div><div class=line>})</div><div class=line>child1.$on(<span class=string>\'test\'</span>, <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>  <span class=built_in>console</span>.log(<span class=string>\'child1 notified\'</span>)</div><div class=line>})</div><div class=line>child2.$on(<span class=string>\'test\'</span>, <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>  <span class=built_in>console</span>.log(<span class=string>\'child2 notified\'</span>)</div><div class=line>})</div><div class=line></div><div class=line>child2.$dispatch(<span class=string>\'test\'</span>)</div><div class=line><span class=comment>// -&gt; "child2 notified"</span></div><div class=line><span class=comment>// -&gt; "child1 notified"</span></div><div class=line><span class=comment>// 没有通知 parent，因为 child1 的回调没有返回 true</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/components.html#父子组件通信>父子组件通信</a></p> </li> </ul> <h3 id=vm-broadcast>vm.$broadcast( event, [...args] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} event</code></li> <li><code>[...args]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>广播事件，通知给当前实例的全部后代。因为后代有多个枝杈，事件将沿着各“路径”通知。每条路径上的通知在触发一个监听器后停止，除非它返回 <code>true</code>。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> parent = <span class=keyword>new</span> Vue()</div><div class=line><span class=comment>// child1 和 child2 是兄弟</span></div><div class=line><span class=keyword>var</span> child1 = <span class=keyword>new</span> Vue({ parent: parent })</div><div class=line><span class=keyword>var</span> child2 = <span class=keyword>new</span> Vue({ parent: parent })</div><div class=line><span class=comment>// child3 在 child2 内</span></div><div class=line><span class=keyword>var</span> child3 = <span class=keyword>new</span> Vue({ parent: child2 })</div><div class=line></div><div class=line>child1.$on(<span class=string>\'test\'</span>, <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>  <span class=built_in>console</span>.log(<span class=string>\'child1 notified\'</span>)</div><div class=line>})</div><div class=line>child2.$on(<span class=string>\'test\'</span>, <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>  <span class=built_in>console</span>.log(<span class=string>\'child2 notified\'</span>)</div><div class=line>})</div><div class=line>child3.$on(<span class=string>\'test\'</span>, <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>  <span class=built_in>console</span>.log(<span class=string>\'child3 notified\'</span>)</div><div class=line>})</div><div class=line></div><div class=line>parent.$broadcast(<span class=string>\'test\'</span>)</div><div class=line><span class=comment>// -&gt; "child1 notified"</span></div><div class=line><span class=comment>// -&gt; "child2 notified"</span></div><div class=line><span class=comment>// 没有通知 child3，因为 child2 的回调没有返回 true</span></div></pre></td></tr></table></figure></li> </ul> <h2 id=-dom>实例方法 / DOM</h2> <h3 id=vm-appendTo>vm.$appendTo( elementOrSelector, [callback] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Element | String} elementOrSelector</code></li> <li><code>{Function} [callback]</code></li> </ul> </li> <li><p><strong>返回值：</strong> <code>vm</code>——实例自身</p> </li> <li><p><strong>用法：</strong></p> <p>将实例的 DOM 元素或片断插入目标元素内。第一个参数可以是一个元素或选择器字符串。如果有过渡则触发过渡。回调在过渡完成后执行，如果没有触发过渡则立即执行。</p> </li> </ul> <h3 id=vm-before>vm.$before( elementOrSelector, [callback] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Element | String} elementOrSelector</code></li> <li><code>{Function} [callback]</code></li> </ul> </li> <li><p><strong>返回值：</strong> <code>vm</code>——实例自身</p> </li> <li><p><strong>用法：</strong></p> <p>将实例的 DOM 元素或片断插到目标元素的前面。第一个参数可以是一个元素或选择器字符串。如果有过渡则触发过渡。回调在过渡完成后执行，如果没有触发过渡则立即执行。</p> </li> </ul> <h3 id=vm-after>vm.$after( elementOrSelector, [callback] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Element | String} elementOrSelector</code></li> <li><code>{Function} [callback]</code></li> </ul> </li> <li><p><strong>返回值：</strong> <code>vm</code>——实例自身</p> </li> <li><p><strong>用法：</strong></p> <p>将实例的 DOM 元素或片断插到目标元素的后面。第一个参数可以是一个元素或选择器字符串。如果有过渡则触发过渡。回调在过渡完成后执行，如果没有触发过渡则立即执行。</p> </li> </ul> <h3 id=vm-remove>vm.$remove( [callback] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Function} [callback]</code></li> </ul> </li> <li><p><strong>返回值：</strong> <code>vm</code>——实例自身</p> </li> <li><p><strong>用法：</strong></p> <p>从 DOM 中删除实例的 DOM 元素或片断。如果有过渡则触发过渡。回调在过渡完成后执行，如果没有触发过渡则立即执行。</p> </li> </ul> <h3 id=vm-nextTick>vm.$nextTick( callback )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Function} [callback]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>将回调延迟到下次 DOM 更新循环之后执行。在修改数据之后立即使用它，然后等待 DOM 更新。它跟全局方法 <code>Vue.nextTick</code> 一样，不同的是回调的 <code>this</code> 自动绑定到调用它的实例上。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>new</span> Vue({</div><div class=line>  <span class=comment>// ...</span></div><div class=line>  methods: {</div><div class=line>    <span class=comment>// ...</span></div><div class=line>    example: <span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>      <span class=comment>// 修改数据</span></div><div class=line>      <span class=keyword>this</span>.message = <span class=string>\'changed\'</span></div><div class=line>      <span class=comment>// DOM 还没有更新</span></div><div class=line>      <span class=keyword>this</span>.$nextTick(<span class=function><span class=keyword>function</span> (<span class=params></span>) </span>{</div><div class=line>        <span class=comment>// DOM 现在更新了</span></div><div class=line>        <span class=comment>// `this` 绑定到当前实例</span></div><div class=line>        <span class=keyword>this</span>.doSomethingElse()</div><div class=line>      })</div><div class=line>    }</div><div class=line>  }</div><div class=line>})</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong></p> <ul> <li><a href=#Vue-nextTick>Vue.nextTick</a></li> <li><a href=/guide/reactivity.html#异步更新队列>异步更新队列</a></li> </ul> </li> </ul> <h2 id=->实例方法 / 生命周期</h2> <h3 id=vm-mount>vm.$mount( [elementOrSelector] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Element | String} [elementOrSelector]</code></li> </ul> </li> <li><p><strong>返回值：</strong> <code>vm</code>——实例自身</p> </li> <li><p><strong>用法：</strong></p> <p>如果 Vue 实例在实例化时没有收到 <code>el</code> 选项，则它处于“未挂载”状态，没有关联的 DOM 元素或片断。可以使用 <code>vm.$mount()</code> 手动地开始挂载/编译未挂载的实例。</p> <p>如果没有参数，模板将被创建为文档之外的的片断，需要手工用其它的 DOM 实例方法把它插入文档中。如果 <code>replace</code> 选项为 <code>false</code>，则自动创建一个空 <code>&lt;div&gt;</code>，作为包装元素。</p> <p>在已经挂载的实例上调用 <code>$mount()</code> 没有效果。这个方法返回实例自身，因而可以链式调用其它实例方法。</p> </li> </ul> <ul> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> MyComponent = Vue.extend({</div><div class=line>  template: <span class=string>\'&lt;div&gt;Hello!&lt;/div&gt;\'</span></div><div class=line>})</div><div class=line></div><div class=line><span class=comment>// 创建并挂载到 #app (会替换 #app)</span></div><div class=line><span class=keyword>new</span> MyComponent().$mount(<span class=string>\'#app\'</span>)</div><div class=line></div><div class=line><span class=comment>// 同上</span></div><div class=line><span class=keyword>new</span> MyComponent({ el: <span class=string>\'#app\'</span> })</div><div class=line></div><div class=line><span class=comment>// 手动挂载</span></div><div class=line><span class=keyword>new</span> MyComponent().$mount().$appendTo(<span class=string>\'#container\'</span>)</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/instance.html#生命周期图示>生命周期图示</a></p> </li> </ul> <h3 id=vm-destroy>vm.$destroy( [remove] )</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Boolean} [remove] - default: false</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>完全销毁实例。清理它与其它实例的连接，解绑它的全部指令及事件监听器，如果 <code>remove</code> 参数是 <code>true</code>，则从 DOM 中删除它关联的 DOM 元素或片断。</p> <p>触发 <code>beforeDestroy</code> 和 <code>destroyed</code> 钩子。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/instance.html#生命周期图示>生命周期图示</a></p> </li> </ul> <h2 id=->指令</h2> <h3 id=v-text>v-text</h3> <ul> <li><p><strong>类型：</strong> <code>String</code></p> </li> <li><p><strong>详细：</strong></p> <p>更新元素的 <code>textContent</code>。</p> <p>在内部， <code>{{ Mustache }}</code> 插值也被编译为 textNode 的一个 <code>v-text</code> 指令。这个指令需要一个包装元素，不过性能稍好并且避免 FOUC (Flash of Uncompiled Content) 。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>span</span> <span class=attr>v-text</span>=<span class=string>"msg"</span>&gt;</span><span class=tag>&lt;/<span class=name>span</span>&gt;</span></div><div class=line><span class=comment>&lt;!-- same as --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>span</span>&gt;</span>{{msg}}<span class=tag>&lt;/<span class=name>span</span>&gt;</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=v-html>v-html</h3> <ul> <li><p><strong>类型：</strong> <code>String</code></p> </li> <li><p><strong>详细：</strong></p> <p>更新元素的 <code>innerHTML</code>。内容按普通 HTML 插入——数据绑定被忽略。如果想复用模板片断，应当使用 <a href=#partial>partials</a>。</p> <p>在内部， <code>{{{ Mustache }}}</code> 插值也会被编译为锚节点上的一个 <code>v-html</code> 指令。这个指令需要一个包装元素，不过性能稍好并且避免 FOUC (Flash of Uncompiled Content) 。</p> <p class=tip>在网站上动态渲染任意 HTML 是非常危险的，因为容易导致 <a href=https://en.wikipedia.org/wiki/Cross-site_scripting>XSS 攻击</a>。只在可信内容上使用 <code>v-html</code>，<strong>永不</strong>用在用户提交的内容上。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-html</span>=<span class=string>"html"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div><div class=line><span class=comment>&lt;!-- 相同 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>div</span>&gt;</span>{{{html}}}<span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=v-if>v-if</h3> <ul> <li><p><strong>类型：</strong> <code>*</code></p> </li> <li><p><strong>用法：</strong></p> <p>根据表达式的值的真假条件渲染元素。在切换时元素及它的数据绑定 / 组件被销毁并重建。如果元素是 <code>&lt;template&gt;</code>，将提出它的内容作为条件块。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/conditional.html>条件渲染</a></p> </li> </ul> <h3 id=v-show>v-show</h3> <ul> <li><p><strong>类型：</strong> <code>*</code></p> </li> <li><p><strong>用法：</strong></p> <p>根据表达式的值的真假切换元素的 <code>display</code> CSS 属性，如果有过渡将触发它。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/conditional.html#v-show>条件渲染 - v-show</a></p> </li> </ul> <h3 id=v-else>v-else</h3> <ul> <li><p><strong>不需要表达式</strong></p> </li> <li><p><strong>限制：</strong> 前一兄弟元素必须有 <code>v-if</code> 或 <code>v-show</code>。</p> </li> <li><p><strong>用法：</strong></p> <p>为 <code>v-if</code> 和 <code>v-show</code> 添加 “else 块”。</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-if</span>=<span class=string>"Math.random() &gt; 0.5"</span>&gt;</span></div><div class=line>  Sorry</div><div class=line><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-else</span>&gt;</span></div><div class=line>  Not sorry</div><div class=line><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/conditional.html#v-else>条件渲染 - v-else</a></p> </li> </ul> <h3 id=v-for>v-for</h3> <ul> <li><p><strong>类型：</strong> <code>Array | Object | Number | String</code></p> </li> <li><p><strong>Param Attributes:</strong></p> <ul> <li><a href=/guide/list.html#track-by><code>track-by</code></a></li> <li><a href=/guide/transitions.html#渐变过渡><code>stagger</code></a></li> <li><a href=/guide/transitions.html#渐变过渡><code>enter-stagger</code></a></li> <li><a href=/guide/transitions.html#渐变过渡><code>leave-stagger</code></a></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>基于源数据将元素或模板块重复数次。指令的值必须使用特定语法 <code>alias (in|of) expression</code>，为当前遍历的元素提供别名：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-for</span>=<span class=string>"item in items"</span>&gt;</span></div><div class=line>  {{ item.text }}</div><div class=line><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure><p>1.0.17+ 支持 <code>of</code> 分隔符。</p> <p>另外也可以为数组索引指定别名（如果值是对象可以为键指定别名）：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-for</span>=<span class=string>"(index, item) in items"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-for</span>=<span class=string>"(key, val) in object"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/list.html>列表渲染</a></p> </li> </ul> <h3 id=v-on>v-on</h3> <ul> <li><p><strong>缩写：</strong> <code>@</code></p> </li> <li><p><strong>类型：</strong> <code>Function | Inline Statement</code></p> </li> <li><p><strong>参数：</strong> <code>event (required)</code></p> </li> <li><p><strong>修饰符：</strong></p> <ul> <li><code>.stop</code> - 调用 <code>event.stopPropagation()</code>。</li> <li><code>.prevent</code> - 调用 <code>event.preventDefault()</code>。</li> <li><code>.capture</code> - 添加事件侦听器时使用 capture 模式。</li> <li><code>.self</code> - 只当事件是从侦听器绑定的元素本身触发时才触发回调。</li> <li><code>.{keyCode | keyAlias}</code> - 只在指定按键上触发回调。</li> </ul> </li> <li><p><strong>用法：</strong></p> <p>绑定事件监听器。事件类型由参数指定。表达式可以是一个方法的名字或一个内联语句，如果没有修饰符也可以省略。</p> <p>用在普通元素上时，只能监听<strong>原生 DOM 事件</strong>。用在自定义元素组件上时，也可以监听子组件触发的<strong>自定义事件</strong>。</p> <p>在监听原生 DOM 事件时，方法以事件为唯一的参数。如果使用内联语句，语句可以访问一个 <code>$event</code> 属性： <code>v-on:click=&quot;handle(&#39;ok&#39;, $event)&quot;</code>。</p> <p><strong>1.0.11+</strong> 在监听自定义事件时，内联语句可以访问一个 <code>$arguments</code> 属性，它是一个数组，包含传给子组件的 <code>$emit</code> 回调的参数。</p> </li> </ul> <ul> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=comment>&lt;!-- 方法处理器 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>button</span> <span class=attr>v-on:click</span>=<span class=string>"doThis"</span>&gt;</span><span class=tag>&lt;/<span class=name>button</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 内联语句 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>button</span> <span class=attr>v-on:click</span>=<span class=string>"doThat(\'hello\', $event)"</span>&gt;</span><span class=tag>&lt;/<span class=name>button</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 缩写 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>button</span> @<span class=attr>click</span>=<span class=string>"doThis"</span>&gt;</span><span class=tag>&lt;/<span class=name>button</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 停止冒泡 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>button</span> @<span class=attr>click.stop</span>=<span class=string>"doThis"</span>&gt;</span><span class=tag>&lt;/<span class=name>button</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 阻止默认行为 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>button</span> @<span class=attr>click.prevent</span>=<span class=string>"doThis"</span>&gt;</span><span class=tag>&lt;/<span class=name>button</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 阻止默认行为，没有表达式 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>form</span> @<span class=attr>submit.prevent</span>&gt;</span><span class=tag>&lt;/<span class=name>form</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 串联修饰符 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>button</span> @<span class=attr>click.stop.prevent</span>=<span class=string>"doThis"</span>&gt;</span><span class=tag>&lt;/<span class=name>button</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 键修饰符，键别名 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>input</span> @<span class=attr>keyup.enter</span>=<span class=string>"onEnter"</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 键修饰符，键代码 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>input</span> @<span class=attr>keyup.13</span>=<span class=string>"onEnter"</span>&gt;</span></div></pre></td></tr></table></figure><p>在子组件上监听自定义事件（当子组件触发 &quot;my-event&quot; 时将调用事件处理器）：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>my-component</span> @<span class=attr>my-event</span>=<span class=string>"handleThis"</span>&gt;</span><span class=tag>&lt;/<span class=name>my-component</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 内联语句 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>my-component</span> @<span class=attr>my-event</span>=<span class=string>"handleThis(123, $arguments)"</span>&gt;</span><span class=tag>&lt;/<span class=name>my-component</span>&gt;</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/events.html>方法与事件处理器</a></p> </li> </ul> <h3 id=v-bind>v-bind</h3> <ul> <li><p><strong>缩写：</strong> <code>:</code></p> </li> <li><p><strong>类型：</strong> <code>* (with argument) | Object (without argument)</code></p> </li> <li><p><strong>参数：</strong> <code>attrOrProp (optional)</code></p> </li> <li><p><strong>修饰符：</strong></p> <ul> <li><code>.sync</code> - 双向绑定，只能用于 prop 绑定。</li> <li><code>.once</code> - 单次绑定，只能用于 prop 绑定。</li> <li><code>.camel</code> - 将绑定的特性名字转回驼峰命名。只能用于普通 HTML 特性的绑定，通常用于绑定用驼峰命名的 SVG 特性，比如 <code>viewBox</code>。</li> </ul> </li> <li><p><strong>用法：</strong></p> <p>动态地绑定一个或多个 attribute，或一个组件 prop 到表达式。</p> <p>在绑定 <code>class</code> 或 <code>style</code> 时，支持其它类型的值，如数组或对象。</p> <p>在绑定 prop 时，prop 必须在子组件中声明。可以用修饰符指定不同的绑定类型。</p> <p>没有参数时，可以绑定到一个对象。注意此时 <code>class</code> 和 <code>style</code> 绑定不支持数组和对象。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=comment>&lt;!-- 绑定 attribute --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>img</span> <span class=attr>v-bind:src</span>=<span class=string>"imageSrc"</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 缩写 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>img</span> <span class=attr>:src</span>=<span class=string>"imageSrc"</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 绑定 class --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>:class</span>=<span class=string>"{ red: isRed }"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>:class</span>=<span class=string>"[classA, classB]"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>:class</span>=<span class=string>"[classA, { classB: isB, classC: isC }]"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 绑定 style --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>:style</span>=<span class=string>"{ fontSize: size + \'px\' }"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>:style</span>=<span class=string>"[styleObjectA, styleObjectB]"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 绑定到一个对象 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-bind</span>=<span class=string>"{ id: someProp, \'other-attr\': otherProp }"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- prop 绑定，"prop" 必须在 my-component 组件内声明 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>my-component</span> <span class=attr>:prop</span>=<span class=string>"someThing"</span>&gt;</span><span class=tag>&lt;/<span class=name>my-component</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 双向 prop 绑定 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>my-component</span> <span class=attr>:prop.sync</span>=<span class=string>"someThing"</span>&gt;</span><span class=tag>&lt;/<span class=name>my-component</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 单次 prop 绑定 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>my-component</span> <span class=attr>:prop.once</span>=<span class=string>"someThing"</span>&gt;</span><span class=tag>&lt;/<span class=name>my-component</span>&gt;</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong></p> <ul> <li><a href=/guide/class-and-style.html>Class 和 Style 绑定</a></li> <li><a href=/guide/components.html#Props>组件 Props</a></li> </ul> </li> </ul> <h3 id=v-model>v-model</h3> <ul> <li><p><strong>类型：</strong> 随表单控件类型不同而不同。</p> </li> <li><p><strong>限制：</strong></p> <ul> <li><code>&lt;input&gt;</code></li> <li><code>&lt;select&gt;</code></li> <li><code>&lt;textarea&gt;</code></li> </ul> </li> <li><p><strong>Param Attributes:</strong></p> <ul> <li><a href=/guide/forms.html#lazy><code>lazy</code></a></li> <li><a href=/guide/forms.html#number><code>number</code></a></li> <li><a href=/guide/forms.html#debounce><code>debounce</code></a></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>在表单控件上创建双向绑定。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/forms.html>表单控件绑定</a></p> </li> </ul> <h3 id=v-ref>v-ref</h3> <ul> <li><p><strong>不需要表达式</strong></p> </li> <li><p><strong>限制：</strong> 子组件</p> </li> <li><p><strong>参数：</strong> <code>id (required)</code></p> </li> <li><p><strong>用法：</strong></p> <p>在父组件上注册一个子组件的索引，便于直接访问。不需要表达式。必须提供参数 id。可以通过父组件的 <code>$refs</code> 对象访问子组件。</p> <p>在和 <code>v-for</code> 一起用时，注册的值将是一个数组，包含所有的子组件，对应于绑定数组。如果 <code>v-for</code> 用在一个对象上，注册的值将是一个对象，包含所有的子组件，对应于绑定对象。</p> </li> <li><p><strong>注意：</strong></p> <p>因为 HTML 不区分大小写，camelCase 名字比如 <code>v-ref:someRef</code> 将全转为小写。可以用 <code>v-ref:some-ref</code> 设置 <code>this.$els.someRef</code>。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>comp</span> <span class=attr>v-ref:child</span>&gt;</span><span class=tag>&lt;/<span class=name>comp</span>&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>comp</span> <span class=attr>v-ref:some-child</span>&gt;</span><span class=tag>&lt;/<span class=name>comp</span>&gt;</span></div></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 从父组件访问</span></div><div class=line><span class=keyword>this</span>.$refs.child</div><div class=line><span class=keyword>this</span>.$refs.someChild</div></pre></td></tr></table></figure><p>使用 <code>v-for</code>：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>comp</span> <span class=attr>v-ref:list</span> <span class=attr>v-for</span>=<span class=string>"item in list"</span>&gt;</span><span class=tag>&lt;/<span class=name>comp</span>&gt;</span></div></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 值是一个数组</span></div><div class=line><span class=keyword>this</span>.$refs.list</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/components.html#子组件索引>子组件索引</a></p> </li> </ul> <h3 id=v-el>v-el</h3> <ul> <li><p><strong>不需要表达式</strong></p> </li> <li><p><strong>参数：</strong> <code>id</code>（必需）</p> </li> <li><p><strong>用法：</strong></p> <p>为 DOM 元素注册一个索引，方便通过所属实例的 <code>$els</code> 访问这个元素。</p> </li> <li><p><strong>注意：</strong></p> <p>因为 HTML 不区分大小写，camelCase 名字比如 <code>v-el:someEl</code> 将转为全小写。可以用 <code>v-el:some-el</code> 设置 <code>this.$els.someEl</code>。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>span</span> <span class=attr>v-el:msg</span>&gt;</span>hello<span class=tag>&lt;/<span class=name>span</span>&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>span</span> <span class=attr>v-el:other-msg</span>&gt;</span>world<span class=tag>&lt;/<span class=name>span</span>&gt;</span></div></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>this</span>.$els.msg.textContent <span class=comment>// -&gt; "hello"</span></div><div class=line><span class=keyword>this</span>.$els.otherMsg.textContent <span class=comment>// -&gt; "world"</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=v-pre>v-pre</h3> <ul> <li><p><strong>不需要表达式</strong></p> </li> <li><p><strong>用法：</strong></p> <p>跳过编译这个元素和它的子元素。可以用来显示原始 Mustache 标签。跳过大量没有指令的节点会加快编译。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>span</span> <span class=attr>v-pre</span>&gt;</span>{{ this will not be compiled }}<span class=tag>&lt;/<span class=name>span</span>&gt;</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=v-cloak>v-cloak</h3> <ul> <li><p><strong>不需要表达式</strong></p> </li> <li><p><strong>用法：</strong></p> <p>这个指令保持在元素上直到关联实例结束编译。和 CSS 规则如 <code>[v-cloak] { display: none }</code> 一起用时，这个指令可以隐藏未编译的 Mustache 标签直到实例准备完毕。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight css"><table><tr><td class=code><pre><div class=line><span class=selector-attr>[v-cloak]</span> {</div><div class=line>  <span class=attribute>display</span>: none;</div><div class=line>}</div></pre></td></tr></table></figure><figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-cloak</span>&gt;</span></div><div class=line>  {{ message }}</div><div class=line><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure><p><code>&lt;div&gt;</code> 不会显示，直到编译结束。</p> </li> </ul> <h2 id=->特殊元素</h2> <h3 id=component>component</h3> <ul> <li><p><strong>特性：</strong></p> <ul> <li><code>is</code></li> <li><a href=/guide/components.html#keep-alive><code>keep-alive</code></a></li> <li><a href=/guide/components.html#transition-mode><code>transition-mode</code></a></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>另一种调用组件的语法。主要是和 <code>is</code> 特性一起用于动态组件。</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=comment>&lt;!-- 动态组件 --&gt;</span></div><div class=line><span class=comment>&lt;!-- 由实例的 `componentId` 属性控制 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>component</span> <span class=attr>:is</span>=<span class=string>"componentId"</span>&gt;</span><span class=tag>&lt;/<span class=name>component</span>&gt;</span></div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/components.html#动态组件>动态组件</a></p> </li> </ul> <h3 id=slot>slot</h3> <ul> <li><p><strong>特性：</strong></p> <ul> <li><code>name</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p><code>&lt;slot&gt;</code> 元素作为组件模板之中的内容分发插槽。这个元素自身将被替换。</p> <p>有 <code>name</code> 特性的 slot 称为命名 slot。 有 <code>slot</code> 特性的内容将分发到名字相匹配的命名 slot。</p> </li> <li><p><strong>另见：</strong> <a href=/guide/components.html#使用-Slot-分发内容>使用 slot 分发内容</a></p> </li> </ul> <h3 id=partial>partial</h3> <ul> <li><p><strong>特性：</strong></p> <ul> <li><code>name</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p><code>&lt;partial&gt;</code> 元素是已注册的 partial 的插槽，partial 在插入时被 Vue 编译。 <code>&lt;partial&gt;</code> 元素本身会被替换。<code>&lt;partial&gt;</code> 元素需要指定 <code>name</code> 特性。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=comment>// 注册 partial</span></div><div class=line>Vue.partial(<span class=string>\'my-partial\'</span>, <span class=string>\'&lt;p&gt;This is a partial! {{msg}}&lt;/p&gt;\'</span>)</div></pre></td></tr></table></figure><figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=comment>&lt;!-- 静态 partial --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>partial</span> <span class=attr>name</span>=<span class=string>"my-partial"</span>&gt;</span><span class=tag>&lt;/<span class=name>partial</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 动态 partial --&gt;</span></div><div class=line><span class=comment>&lt;!-- 渲染 partial，id === vm.partialId --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>partial</span> <span class=attr>v-bind:name</span>=<span class=string>"partialId"</span>&gt;</span><span class=tag>&lt;/<span class=name>partial</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 动态 partial，使用 v-bind 缩写语法 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>partial</span> <span class=attr>:name</span>=<span class=string>"partialId"</span>&gt;</span><span class=tag>&lt;/<span class=name>partial</span>&gt;</span></div></pre></td></tr></table></figure></li> </ul> <h2 id=->过滤器</h2> <h3 id=capitalize>capitalize</h3> <ul> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line>{{ msg | capitalize }}</div></pre></td></tr></table></figure><p><em>&#39;abc&#39; =&gt; &#39;Abc&#39;</em></p> </li> </ul> <h3 id=uppercase>uppercase</h3> <ul> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line>{{ msg | uppercase }}</div></pre></td></tr></table></figure><p><em>&#39;abc&#39; =&gt; &#39;ABC&#39;</em></p> </li> </ul> <h3 id=lowercase>lowercase</h3> <ul> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line>{{ msg | lowercase }}</div></pre></td></tr></table></figure><p><em>&#39;ABC&#39; =&gt; &#39;abc&#39;</em></p> </li> </ul> <h3 id=currency>currency</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} [symbol] - default: &#39;$&#39;</code></li> </ul> </li> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line>{{ amount | currency }}</div></pre></td></tr></table></figure><p><em>12345 =&gt; $12,345.00</em></p> <p>使用其它符号：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line>{{ amount | currency \'£\' }}</div></pre></td></tr></table></figure><p><em>12345 =&gt; £12,345.00</em></p> </li> </ul> <h3 id=pluralize>pluralize</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{String} single, [double, triple, ...]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>如果只有一个参数，复数形式只是简单地在末尾添加一个 &quot;s&quot;。如果有多个参数，参数被当作一个字符串数组，对应一个、两个、三个...复数词。如果值的个数多于参数的个数，多出的使用最后一个参数。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line>{{count}} {{count | pluralize \'item\'}}</div></pre></td></tr></table></figure><p><em>1 =&gt; &#39;1 item&#39;</em> <em>2 =&gt; &#39;2 items&#39;</em></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line>{{date}}{{date | pluralize \'st\' \'nd\' \'rd\' \'th\'}}</div></pre></td></tr></table></figure><p>结果：</p> <p><em>1 =&gt; &#39;1st&#39;</em> <em>2 =&gt; &#39;2nd&#39;</em> <em>3 =&gt; &#39;3rd&#39;</em> <em>4 =&gt; &#39;4th&#39;</em> <em>5 =&gt; &#39;5th&#39;</em></p> </li> </ul> <h3 id=json>json</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Number} [indent] - 默认值：2</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>输出经 <code>JSON.stringify()</code> 处理后的结果，而不是输出 <code>toString()</code> 的结果（如 <code>[object Object]</code>）。</p> </li> <li><p><strong>示例：</strong></p> <p>以四个空格的缩进打印一个对象：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>pre</span>&gt;</span>{{ nestedObject | json 4 }}<span class=tag>&lt;/<span class=name>pre</span>&gt;</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=debounce>debounce</h3> <ul> <li><p><strong>限制：</strong> 指令的值须是函数，如 <code>v-on</code></p> </li> <li><p><strong>参数：</strong></p> <ul> <li><code>{Number} [wait] - 默认值： 300</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>包装处理器，让它延迟执行 <code>x</code> ms， 默认延迟 300ms。包装后的处理器在调用之后至少将延迟 <code>x</code> ms， 如果在延迟结束前再次调用，延迟时长重置为 <code>x</code> ms。</p> </li> <li><p><strong>示例：</strong></p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>input</span> @<span class=attr>keyup</span>=<span class=string>"onKeyup | debounce 500"</span>&gt;</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=limitby>limitBy</h3> <ul> <li><p><strong>限制：</strong> 指令的值须是数组，如 <code>v-for</code></p> </li> <li><p><strong>参数：</strong></p> <ul> <li><code>{Number} limit</code></li> <li><code>{Number} [offset]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>限制数组为开始 N 个元素，N 由第一个参数指定。第二个参数是可选的，指定开始的偏移量。</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=comment>&lt;!-- 只显示开始 10 个元素 --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-for</span>=<span class=string>"item in items | limitBy 10"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div><div class=line></div><div class=line><span class=comment>&lt;!-- 显示第 5 到 15 元素--&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-for</span>=<span class=string>"item in items | limitBy 10 5"</span>&gt;</span><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=filterby>filterBy</h3> <ul> <li><p><strong>限制：</strong> 指令的值须是数组，如 <code>v-for</code></p> </li> <li><p><strong>参数：</strong></p> <ul> <li><code>{String | Function} targetStringOrFunction</code></li> <li><code>&quot;in&quot; (optional delimiter)</code></li> <li><code>{String} [...searchKeys]</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>返回过滤后的数组。第一个参数可以是字符串或函数。</p> <p>如果第一个参数是字符串，则在每个数组元素中搜索它：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-for</span>=<span class=string>"item in items | filterBy \'hello\'"</span>&gt;</span></div></pre></td></tr></table></figure><p>在上例中，只显示包含字符串 <code>&quot;hello&quot;</code> 的元素。</p> <p>如果 item 是一个对象，过滤器将递归地在它所有属性中搜索。为了缩小搜索范围，可以指定一个搜索字段：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-for</span>=<span class=string>"user in users | filterBy \'Jack\' in \'name\'"</span>&gt;</span></div></pre></td></tr></table></figure><p>在上例中，过滤器只在用户对象的 <code>name</code> 属性中搜索 <code>&quot;Jack&quot;</code>。<strong>为了更好的性能，最好始终限制搜索范围。</strong></p> <p>上例使用静态参数，当然可以使用动态参数作为搜索目标或搜索字段。配合 <code>v-model</code> 我们可以轻松实现输入提示效果：</p> </li> </ul> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>id</span>=<span class=string>"filter-by-example"</span>&gt;</span></div><div class=line>    <span class=tag>&lt;<span class=name>input</span> <span class=attr>v-model</span>=<span class=string>"name"</span>&gt;</span></div><div class=line>    <span class=tag>&lt;<span class=name>ul</span>&gt;</span></div><div class=line>      <span class=tag>&lt;<span class=name>li</span> <span class=attr>v-for</span>=<span class=string>"user in users | filterBy name in \'name\'"</span>&gt;</span></div><div class=line>        {{ user.name }}</div><div class=line>      <span class=tag>&lt;/<span class=name>li</span>&gt;</span></div><div class=line>    <span class=tag>&lt;/<span class=name>ul</span>&gt;</span></div><div class=line><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>new</span> Vue({</div><div class=line>    el: <span class=string>\'#filter-by-example\'</span>,</div><div class=line>    data: {</div><div class=line>      name: <span class=string>\'\'</span>,</div><div class=line>      users: [</div><div class=line>        { name: <span class=string>\'Bruce\'</span> },</div><div class=line>        { name: <span class=string>\'Chuck\'</span> },</div><div class=line>        { name: <span class=string>\'Jackie\'</span> }</div><div class=line>      ]</div><div class=line>    }</div><div class=line>})</div></pre></td></tr></table></figure><p></p><div id=filter-by-example class=demo> <input v-model=name> <ul> <li v-for="user in users | filterBy name in \'name\'"> {{ user.name }} </li> </ul> </div><p></p> <script>new Vue({el:"#filter-by-example",data:{name:"",users:[{name:"Bruce"},{name:"Chuck"},{name:"Jackie"}]}})</script> <ul> <li><p><strong>另一个示例：</strong></p> <p>多搜索字段：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>li</span> <span class=attr>v-for</span>=<span class=string>"user in users | filterBy searchText in \'name\' \'phone\'"</span>&gt;</span><span class=tag>&lt;/<span class=name>li</span>&gt;</span></div></pre></td></tr></table></figure><p>多搜索字段为一个动态数组：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=comment>&lt;!-- fields = [\'fieldA\', \'fieldB\'] --&gt;</span></div><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-for</span>=<span class=string>"user in users | filterBy searchText in fields"</span>&gt;</span></div></pre></td></tr></table></figure><p>使用自定义过滤函数：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>v-for</span>=<span class=string>"user in users | filterBy myCustomFilterFunction"</span>&gt;</span></div></pre></td></tr></table></figure></li> </ul> <h3 id=orderby>orderBy</h3> <ul> <li><p><strong>限制：</strong> 指令的值须是数组，如 <code>v-for</code></p> </li> <li><p><strong>参数：</strong></p> <ul> <li><code>{String | Array&lt;String&gt; | Function} ...sortKeys</code></li> <li><code>{String} [order] - 默认值：1</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>返回排序后的数组。你可以传入多个键名。你也可以传入一个数组，此数组包含排序的键名。如果你想使用自己的排序策略，可以传入一个函数。可选参数 <code>order</code> 决定结果升序（<code>order &gt;= 0</code>）或降序（<code>order &lt; 0</code>）。</p> <p>对于原始类型数组，可以忽略 <code>sortKey</code> ，只提供排序，例如 <code>orderBy 1</code>。</p> </li> <li><p><strong>示例：</strong></p> <p>按名字排序用户：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>ul</span>&gt;</span></div><div class=line>  <span class=tag>&lt;<span class=name>li</span> <span class=attr>v-for</span>=<span class=string>"user in users | orderBy \'name\'"</span>&gt;</span></div><div class=line>    {{ user.name }}</div><div class=line>  <span class=tag>&lt;/<span class=name>li</span>&gt;</span></div><div class=line><span class=tag>&lt;/<span class=name>ul</span>&gt;</span></div></pre></td></tr></table></figure><p>降序：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>ul</span>&gt;</span></div><div class=line>  <span class=tag>&lt;<span class=name>li</span> <span class=attr>v-for</span>=<span class=string>"user in users | orderBy \'name\' -1"</span>&gt;</span></div><div class=line>    {{ user.name }}</div><div class=line>  <span class=tag>&lt;/<span class=name>li</span>&gt;</span></div><div class=line><span class=tag>&lt;/<span class=name>ul</span>&gt;</span></div></pre></td></tr></table></figure><p>原始类型数组：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>ul</span>&gt;</span></div><div class=line>  <span class=tag>&lt;<span class=name>li</span> <span class=attr>v-for</span>=<span class=string>"n in numbers | orderBy true"</span>&gt;</span></div><div class=line>    {{ n }}</div><div class=line>  <span class=tag>&lt;/<span class=name>li</span>&gt;</span></div><div class=line><span class=tag>&lt;/<span class=name>ul</span>&gt;</span></div></pre></td></tr></table></figure><p>动态排序：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>id</span>=<span class=string>"orderby-example"</span>&gt;</span></div><div class=line>  <span class=tag>&lt;<span class=name>button</span> @<span class=attr>click</span>=<span class=string>"order = order * -1"</span>&gt;</span>Reverse Sort Order<span class=tag>&lt;/<span class=name>button</span>&gt;</span></div><div class=line>  <span class=tag>&lt;<span class=name>ul</span>&gt;</span></div><div class=line>    <span class=tag>&lt;<span class=name>li</span> <span class=attr>v-for</span>=<span class=string>"user in users | orderBy \'name\' order"</span>&gt;</span></div><div class=line>      {{ user.name }}</div><div class=line>    <span class=tag>&lt;/<span class=name>li</span>&gt;</span></div><div class=line>  <span class=tag>&lt;/<span class=name>ul</span>&gt;</span></div><div class=line><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>new</span> Vue({</div><div class=line>  el: <span class=string>\'#orderby-example\'</span>,</div><div class=line>  data: {</div><div class=line>    order: <span class=number>1</span>,</div><div class=line>    users: [{ name: <span class=string>\'Bruce\'</span> }, { name: <span class=string>\'Chuck\'</span> }, { name: <span class=string>\'Jackie\'</span> }]</div><div class=line>  }</div><div class=line>})</div></pre></td></tr></table></figure><p>使用两个键名排序：</p> </li> </ul> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>ul</span>&gt;</span></div><div class=line>    <span class=tag>&lt;<span class=name>li</span> <span class=attr>v-for</span>=<span class=string>"user in users | orderBy \'lastName\' \'firstName\'"</span>&gt;</span></div><div class=line>      {{ user.lastName }} {{ user.firstName }}</div><div class=line>    <span class=tag>&lt;/<span class=name>li</span>&gt;</span></div><div class=line><span class=tag>&lt;/<span class=name>ul</span>&gt;</span></div></pre></td></tr></table></figure><p></p><div id=orderby-example class=demo> <button @click="order = order * -1">Reverse Sort Order</button> <ul> <li v-for="user in users | orderBy \'name\' order"> {{ user.name }} </li> </ul> </div><p></p> <script>new Vue({el:"#orderby-example",data:{order:1,users:[{name:"Bruce"},{name:"Chuck"},{name:"Jackie"}]}})</script> <p> 使用一个函数排序：</p> <figure class="highlight html"><table><tr><td class=code><pre><div class=line><span class=tag>&lt;<span class=name>div</span> <span class=attr>id</span>=<span class=string>"orderby-compare-example"</span> <span class=attr>class</span>=<span class=string>"demo"</span>&gt;</span></div><div class=line>    <span class=tag>&lt;<span class=name>button</span> @<span class=attr>click</span>=<span class=string>"order = order * -1"</span>&gt;</span>Reverse Sort Order<span class=tag>&lt;/<span class=name>button</span>&gt;</span></div><div class=line>    <span class=tag>&lt;<span class=name>ul</span>&gt;</span></div><div class=line>      <span class=tag>&lt;<span class=name>li</span> <span class=attr>v-for</span>=<span class=string>"user in users | orderBy ageByTen"</span>&gt;</span></div><div class=line>        {{ user.name }} - {{ user.age }}</div><div class=line>      <span class=tag>&lt;/<span class=name>li</span>&gt;</span></div><div class=line>    <span class=tag>&lt;/<span class=name>ul</span>&gt;</span></div><div class=line><span class=tag>&lt;/<span class=name>div</span>&gt;</span></div></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>new</span> Vue({</div><div class=line>el: <span class=string>\'#orderby-compare-example\'</span>,</div><div class=line>data: {</div><div class=line>  order: <span class=number>1</span>,</div><div class=line>  users: [</div><div class=line>    {</div><div class=line>      name: <span class=string>\'Jackie\'</span>,</div><div class=line>      age: <span class=number>62</span></div><div class=line>    },</div><div class=line>    {</div><div class=line>      name: <span class=string>\'Chuck\'</span>,</div><div class=line>      age: <span class=number>76</span></div><div class=line>    },</div><div class=line>    {</div><div class=line>      name: <span class=string>\'Bruce\'</span>,</div><div class=line>      age: <span class=number>61</span></div><div class=line>    }</div><div class=line>  ]</div><div class=line>},</div><div class=line>methods: {</div><div class=line>  ageByTen: <span class=function><span class=keyword>function</span> (<span class=params>a, b</span>) </span>{</div><div class=line>    <span class=keyword>return</span> <span class=built_in>Math</span>.floor(a.age / <span class=number>10</span>) - <span class=built_in>Math</span>.floor(b.age / <span class=number>10</span>)</div><div class=line>  }</div><div class=line>}</div><div class=line>})</div></pre></td></tr></table></figure><p></p><div id=orderby-compare-example class=demo> <button @click="order = order * -1">Reverse Sort Order</button> <ul id=orderby-compare-example> <template v-for="user in users | orderBy ageByTen order"> {{ user.name }} - {{ user.age }} </template> </ul> </div><p></p> <script>new Vue({el:"#orderby-compare-example",data:{order:1,users:[{name:"Jackie",age:62},{name:"Chuck",age:76},{name:"Bruce",age:61}]},methods:{ageByTen:function(e,a){return Math.floor(e.age/10)-Math.floor(a.age/10)}}})</script> <h2 id=->数组扩展方法</h2> <p>Vue.js 在 <code>Array.prototype</code> 上添加了两个方法，以方便常见的数组操作，并且能触发视图更新。</p> <h3 id=array-set-index-value->array.$set(index, value)</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Number} index</code></li> <li><code>{*} value</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>通过索引设置数组元素并触发视图更新。</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line>vm.animals.$set(<span class=number>0</span>, { name: <span class=string>\'Aardvark\'</span> })</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/list.html#问题>数组检测问题</a></p> </li> </ul> <h3 id=array-remove-reference->array.$remove(reference)</h3> <ul> <li><p><strong>参数：</strong></p> <ul> <li><code>{Reference} reference</code></li> </ul> </li> <li><p><strong>用法：</strong></p> <p>通过索引删除数组元素并触发视图更新。这个方法先在数组中搜索这个元素，如果找到了则调用 <code>array.splice(index, 1)</code>。</p> <figure class="highlight js"><table><tr><td class=code><pre><div class=line><span class=keyword>var</span> aardvark = vm.animals[<span class=number>0</span>]</div><div class=line>vm.animals.$remove(aardvark)</div></pre></td></tr></table></figure></li> <li><p><strong>另见：</strong> <a href=/guide/list.html#变异方法>变异方法</a> </p> </li> </ul>';
}});